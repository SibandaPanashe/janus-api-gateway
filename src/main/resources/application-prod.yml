spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5433}/${POSTGRES_DB:janus}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

janus:
  jwt:
    secret: ${JANUS_JWT_SECRET}